query findRecipe($slug: String!) {
  recipes(where: { slug: $slug }) {
    name
    number_of_servings
    time
    steps {
      step
    }
    ingredients {
      ingredient
    }
    users_permissions_users {
      username
    }
  }
}
